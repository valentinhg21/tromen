/* window.addEventListener('DOMContentLoaded', e => {
    const myLocation = document.getElementById('mapFlyLocation');
  

    myLocation.addEventListener('click', e => {
        geoLocation(map);
    })
}) */